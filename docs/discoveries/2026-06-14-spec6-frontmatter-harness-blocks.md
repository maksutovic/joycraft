# Spec 6 deviation: engine extended again to support harness blocks in frontmatter

- Spec 6 said "MUST NOT invent new template variables" and confined block placement to skill bodies, but `joycraft-implement-feature` baseline has three divergent `description:` strings ("fresh-context subagent per spec" vs "sequential chain" vs "delegates to the joycraft-implement-loop driver") that no single var can express — extended `scripts/lib/skill-template.mjs` to run `processHarnessBlocks` on frontmatter too (one extra line), continuing the spec 5 engine-extension pattern. See `src/skills/joycraft-implement-feature.md` frontmatter and the engine call site in `applyTemplate`.
- Spec 6 also said "fold claude-only 'Scanning Prior Research' section into canonical (universal — all harnesses get it)" for `joycraft-research`, but that contradicts the dominant "byte-identical to baseline" acceptance criterion (it would add ~9 lines to codex/pi variants). Kept the section inside the claude block — byte-identical wins. Surface to user if this needs revisiting before PR.

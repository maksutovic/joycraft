Spec missed two existing contracts: `tests/harness-selection.test.ts` and `tests/upgrade.test.ts` asserted init/upgrade leave an existing AGENTS.md byte-identical (updated to "plus the D14 row"), and `generateAgentsMd` is never the designated memory file (claude-only's companion AGENTS.md; the multiTool AGENTS.md comes from `generateCLAUDEMd`), so its new flag stays off in production.
See `docs/features/2026-09-22-fable-native-sdlc-harness/specs/point-memory-file-at-profile.md`, `src/update-inventory.ts` (`contextMapPointerPatch`, `generatorContent`); the gate list lives in `src/model-profile.ts`, not `src/bundle-inventory.ts`, to keep improve-claude-md free of the bundled-files import.

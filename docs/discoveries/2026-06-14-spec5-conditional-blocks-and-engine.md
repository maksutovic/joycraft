# Spec 5 deviations: conditional blocks added for 3 escalation skills + engine extended for frontmatter substitution

- Spec 5 said "MUST NOT add `<!-- harness:NAME -->` blocks" but `joycraft-implement`, `joycraft-spec-done`, `joycraft-decompose` had pi-only machinery (loop-iteration check / Pi isolated-mode loop paragraph / Step 7 Hand Off w/ joycraft-implement-loop) that would be dropped under pure unify-on-claude — added 3 conditional blocks rather than re-classify to spec 6, since driver task said complete WHOLE spec. See `docs/features/2026-06-11-single-source-skills/specs/migrate-dirty-unify.md` and `src/skills/joycraft-{implement,spec-done,decompose}.md`.
- Engine spec only covered body-level `{{var}}` substitution but skill frontmatter `description:` fields reference `{{boundary_file}}` and `{{skill_prefix}}` — extended `scripts/lib/skill-template.mjs` to substitute in frontmatter too, and added 3 tests in `tests/skill-template.test.ts`. Also extended block-line whitespace handling so stripped blocks don't leave blank-line residue. `tests/generate-bundled-files.test.ts` now restores src/skills after wipe (prior test assumed empty canonical dir).

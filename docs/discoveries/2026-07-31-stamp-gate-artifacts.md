# 2026-07-31 — stamp-gate-artifacts

The spec's Affected Files listed `joycraft-bugfix.md`, but bugfix has no render/open step to stamp — the six stamped gates are interview, new-feature, tune, design, decide, decompose. Also `init`/`upgrade` already shipped `REVIEW_GATE_TEMPLATE.html` via the TEMPLATES record; only the repo copy at `docs/templates/` was missing.

# Remove AGENTS.md "See CLAUDE.md" Cross-Reference — Atomic Spec

> **Parent Brief:** `docs/features/2026-05-27-joycraft-workflow-fixes/brief.md`
> **Status:** Complete
> **Date:** 2026-05-27
> **Estimated scope:** 1 session / 2 files / ~10 lines

---

## What

Verify that `src/agents-md.ts` no longer emits "See CLAUDE.md for detailed instructions" and update the test suite to assert the standalone output. The source code appears to already have the fix applied — this spec ensures it's verified and tested.

## Why

AGENTS.md is consumed by Pi and Codex — neither of which has CLAUDE.md. The cross-reference makes AGENTS.md a second-class citizen. Both files should work independently.

## Acceptance Criteria
- [ ] `generateAgentsMd()` output does NOT contain "See CLAUDE.md" or "CLAUDE.md"
- [ ] `generateAgentsMd()` output contains "> Auto-generated by Joycraft." (standalone, no cross-ref)
- [ ] Test suite asserts absence of "CLAUDE.md" in AGENTS.md output
- [ ] Build passes (`pnpm build`)
- [ ] Tests pass (`pnpm test`)
- [ ] Typecheck passes (`pnpm typecheck`)

## Test Plan

| Acceptance Criterion | Test | Type |
|---------------------|------|------|
| No CLAUDE.md reference | grep generated output for "CLAUDE.md" → empty | unit |
| Standalone attribution | grep generated output for "Auto-generated by Joycraft." → found | unit |

**Execution order:**
1. Add test asserting AGENTS.md output has NO "CLAUDE.md" reference
2. Run tests — they should PASS (source already fixed)
3. If source still has the old line, remove it and re-test

**Smoke test:** `grep -c 'CLAUDE.md'` on `generateAgentsMd()` output — instant pass/fail.

**Before implementing, verify your test harness:**
1. Run all tests — any that fail should be because tests are wrong, not source
2. Each test calls `generateAgentsMd()` with a real StackInfo
3. Smoke test runs in milliseconds

## Constraints
- MUST: Do NOT change the AGENTS.md section-merging logic — only verify/fix the one attribution line
- MUST NOT: Remove the "Auto-generated by Joycraft." line entirely

## Affected Files
| Action | File | What Changes |
|--------|------|-------------|
| VERIFY | `src/agents-md.ts` | Confirm "See CLAUDE.md" is already removed |
| ADD/EDIT | `tests/` (relevant test file) | Add assertion that AGENTS.md output has no CLAUDE.md reference |

## Approach

Check if `src/agents-md.ts` still contains the old line:
```
> Auto-generated by Joycraft. See CLAUDE.md for detailed instructions.
```

If it does, change to:
```
> Auto-generated by Joycraft.
```

If it's already fixed (likely — the external API safety section was also added), add the missing test assertion and move on.

**Rejected alternative:** Removing the attribution line entirely. The line is useful — it tells users Joycraft generated the file and they shouldn't manually edit sections it manages.

## Edge Cases
| Scenario | Expected Behavior |
|----------|------------------|
| AGENTS.md already has a custom user section | Merge logic preserves user content, only changes the attribution |
| Project has both AGENTS.md and CLAUDE.md | Both files work independently, no cross-references in either direction |

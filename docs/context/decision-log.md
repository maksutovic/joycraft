---
last_updated: 2026-08-11
last_updated_by: Maximilian Maksutovic
---

# Decision Log

> Why choices were made, not just what was chosen.

## Decisions

| Date | Decision | Why | Alternatives Rejected | Revisit When |
| 2026-08-11 | STE D6: the interview playback gate regains a one-line style-contract citation inside this feature (dedicated spec), not later in the succinct-gates queue | Human choice at the decompose gate, overriding the defer recommendation; with D4 covering dialogue, the playback gate must carry the delivery pointer, not just the obligation | Deferring to the succinct-gates queue when its in-review specs graduate (recommended, rejected) | If the succinct-gates in-review interview specs get revised before this lands — coordinate the file edit |
| 2026-08-11 | STE D1 amended (supersedes the same-day append shape): output-style.md is rewritten as one integrated STE rule set (~10 rules merging the prior 8 with STE mechanics), the doc itself written in STE; the template test updates in the same spec, keeping the score-scale and absolute-path bans | Human direction at the decompose gate: STE is the primary house style, not an addon section; an appended section reads as optional and leaves two competing voices in one doc | Append-a-section (the original D1 shape); keeping the 8 rules and weaving STE per-rule (STE reads as woven-in, not the foundation) | If the rewrite destabilizes the output-style test family or user projects push back after an upgrade |
| 2026-08-11 | STE D4 amended (supersedes the same-day three-surface set): the contract governs all human-facing output — gate artifacts, PR bodies, session-end summaries, interview playback, and gate chat/dialogue | Human direction at the decompose gate: STE-styled artifacts and dialogue are the only form of conversation with the user; a surface split re-creates the two-voice problem | Written surfaces only, free-form dialogue (artifact voice and chat voice diverge) | If STE chat proves stilted in live interview sessions |
| 2026-08-11 | STE D1: the shipped house-style doc `src/templates/reference/output-style.md` gains a "Sentence mechanics (STE)" section — the escalation the 2026-07-27 D2 clause reserved | Observed gate jargon (user report 2026-08-11) is the trigger evidence D2 named; ONE_HOME plus the tested pointer network deliver the section with zero skill-body edits; the slop table derives from ASD-STE100 via SimpleEnglish v1.2.0 (MIT), not invented tells | Repo-local guidance only (users keep getting gate jargon); a second reference doc (11 new pointers + a second test family) | If user projects push back on the stricter contract after an upgrade |
| 2026-08-11 | STE D2: ste-lint.py is maintainer-side only — vendored at `scripts/ste-lint.py`, a CI test lints Joycraft's own shipped human-facing template prose; users get manual self-check rules in output-style.md, never a script obligation | Requiring users' agents to run a python script per artifact is obtrusive and out of the spirit of a skill (human rationale, verbatim) | Shipping the linter in templates with an agent-side run step; a harden hook (expands the two-surface contract, python3 hard dependency) | If manual self-check proves insufficient in user projects — then revisit shipping the linter |
| 2026-08-11 | STE D3: self-check is two-tier — fix-to-zero on reliable regex classes (contractions, semicolons, banned modals, Latin abbreviations, slop words), advisory on approximate ones (sentence length, synonym rotation) | Unambiguous where regex is reliable, tolerant where it miscounts | Single violations-per-100w budget (hard violations survive under the average); report-only (advisory reports get ignored) | If the two-tier rule proves too complex to hold in practice |
| 2026-08-11 | STE D4: the STE contract governs the full human-facing set — gate artifacts, PR bodies, session-end summaries | Review-time prose was the original complaint; a gate-only split re-creates the two-scope problem output-style D5 removed | Gate artifacts only in v1 | If the larger test blast radius destabilizes the output-style test family |
| 2026-08-11 | STE D5: vendored-linter attribution = SPDX + upstream URL/version file header + CHANGELOG line; sets the repo's first vendoring precedent | File is repo-internal after D2, so README acknowledgment is unnecessary, but provenance must survive in the script and release notes | Header only (invisible provenance); header + README + CHANGELOG (README ack moot for an unshipped file) | If a future feature ships vendored code to users — then README ack returns |
|------|----------|-----|----------------------|-------------|
| 2026-07-31 | Team-ready-gates D9: Linear ticket creation from PRD — backlogged | Out of this revision's scope; needs its own design pass | Bolting it onto the handoff-slot spec (scope creep) | Backlogged — see docs/backlog/2026-07-31-linear-ticket-creation.md |
| 2026-07-31 | Team-ready-gates D8: Cursor skill discovery — backlogged | Out of this revision's scope; harness support question, not a gate question | Folding Cursor into the copilot variant unverified | Backlogged — see docs/backlog/2026-07-31-cursor-skill-discovery.md |
| 2026-07-31 | Team-ready-gates D13: revision marker is an integer in the rendered HTML footer, read from the previous render and incremented | State stays local to the artifact — no new state file to sync or migrate | A render-count file in docs/.joycraft (second bookkeeping surface for one integer) | If hand-edited footers make the fallback-to-rev-1 path common |
| 2026-07-31 | Team-ready-gates D12: the auto-open on/off setting persists as `autoOpen` in docs/.joycraft/state.json; unknown keys survive rewrites | Reuses the existing version-state file — no new config surface; unknown-key preservation keeps newer Joycrafts' fields safe | A separate settings file (one more file to discover and migrate) | If more per-user settings accrete and outgrow the state file |
| 2026-07-31 | Team-ready-gates D11: every defer-to-person confirms visibly in one line — who, which question, where recorded | Silent file mutation on a conversational shortcut is the known failure mode (discovery 2026-07-20) | Silent recording with a summary at the gate (loses the moment-of-action check) | If the confirmation lines prove noisy in long interviews |
| 2026-07-31 | Team-ready-gates D10: gate question directives require ≥2 real options and route free text as "<choice> because <reason>" (Pattern B) | 3/3 native-UI trials captured rationale cleanly (decision-log 2026-07-20); one-option questions capture nothing | Open prose questions (unanswerable walls); rationale as a separate follow-up question (doubles the question count) | If Pattern B answers stop carrying usable rationale |
| 2026-07-31 | Team-ready-gates D7: README restructured install-first with TOC; SECURITY.md added thin | Enterprise adopters check install path and security posture before reading vision | Full security whitepaper (nothing to back it yet) | When a real security review demands more than the thin doc |
| 2026-07-31 | Team-ready-gates D6: gate HTML carries a timestamp + revision banner; auto-open becomes a persisted setting | Six projects of gate tabs made "which render is latest" unanswerable; auto-open was forced on | Filename-versioned renders (breaks stable links pasted into Notion) | If revision-in-footer proves too subtle and needs UI weight |
| 2026-07-31 | Team-ready-gates D5: the model/effort question skip was a bug — fixed by enumerating tune's offer as four separate directive-routed questions | A bundled prose sentence let trailing questions be reformatted away (observed live 2026-07-31); structure survives where prose does not | Treating it as user error and documenting harder | If enumerated questions still get skipped in the wild |
| 2026-07-31 | Team-ready-gates D4: briefs/PRDs end with a fenced "Prompt for the implementing agent" section in the five-line briefing grammar | Praful hand-built this block for every PRD — the one part of his flow Joycraft didn't generate; the grammar already exists at every handoff | A separate handoff.md artifact (one more file to track across six projects) | If engineers need more than the five lines to start cold |
| 2026-07-31 | Team-ready-gates D3: custom output templates live in docs/templates/output/, checked by exact filename before bundled defaults; machine-required sections always appended | Teams have their own PRD formats; exact-match lookup keeps the behavior predictable and non-magical | Fuzzy template matching (guesses wrong silently); per-skill template config (config surface for a file-drop problem) | If teams ask for per-feature template overrides |
| 2026-07-31 | Team-ready-gates D2: defer-to-person is a first-class gate answer — questions terminate `assigned`, with an assigned section in md and assignee-tagged cards in HTML | Praful assigns questions to people not in the session; answered-or-parked lost assignments across six projects | A per-person questions file (scatters state; the Notion-shared artifact is where the assignee already reads) | If assigned questions rot unanswered and need nudging machinery |
| 2026-07-31 | Team-ready-gates D1: every gate question routes through the harness-native question UI — AskUserQuestion on claude, structured chat fallback on codex/pi/copilot — in all five question-bearing gate skills | The picker appearing only intermittently was the top team-usage complaint; only decide carried the directive before | Leaving it to decide alone (proven insufficient live); interactive HTML capture (violates display-only dossier rule) | If a harness ships a richer native question surface |
| 2026-07-29 | Succinct-gates D7: the execution profile is data only — briefings inject whatever the profile says verbatim; model-tiering's stage-routing defaults stay backlogged (`docs/backlog/2026-07-20-model-tiering.md`) | Injecting is one line per briefing; routing policy is a separate, unproven feature that deserves its own decomposition | Reviving model-tiering inside this feature (scope creep onto an unvalidated design) | When the backlogged model-tiering feature is picked up |
| 2026-07-29 | Succinct-gates D6: the per-project execution profile (swarm opt-in + per-harness model/effort) lives as a sentinel-delimited `## Execution Profile` section in AGENTS.md, generated at init, inserted by upgrade, offered by tune | Every harness already reads AGENTS.md each session — no pointer to follow (the failure mode this feature kills), hand-editable, team-shared via git | A pointer/config file (`docs/.joycraft/config.json` — machine-owned, another indirection); a separate profile doc (one more file to discover) | If AGENTS.md sections prove too fragile to sentinel-parse across user edits |
| 2026-07-29 | Succinct-gates D5: every gate handoff is a fenced copy-pasteable briefing prompt (command, pickup sentence, decided/don't-reopen, Start, Hazard, Done-when), replacing the bare `/clear + command` | The human had been hand-writing these; a cold agent briefed in-prompt outperforms one told only which command to run (stamped 2026-07-29, format chosen from three previews) | Bare command handoff (the failing status quo); a briefing file on disk (another pointer to follow) | If headless drivers make cross-conversation handoffs obsolete |
| 2026-07-29 | Succinct-gates D4: rendered gate HTML is committed + linguist-collapsed; the markdown stays the canonical agent-readable record — agents read .md, humans get HTML | The two-channel split runs all the way down: HTML never replaces the record, it renders it; dossier.html precedent plus `.gitattributes` already solves PR noise | Transient/uncommitted renders (humans lose the artifact after the session); HTML-as-canonical (agents parsing HTML) | If reviewers routinely need the collapsed HTML expanded |
| 2026-07-29 | Succinct-gates D3: tune and optimize join the artifact gates now, despite `entry: agent` | Their reports are heavily human-read regardless of entry point — the artifact decides, not the door (same logic as output-style D7) | Feature-gates only (leaves the two most-read reports as walls of chat text) | If the tune/optimize renders go unopened in practice |
| 2026-07-29 | Succinct-gates D2: chat gate message capped at ~10 lines via a fixed slot set (outcome / artifact path / questions-count / next action) with per-slot caps inline | Caps only work as slot attributes — a standalone "be brief" number is gameable; ten lines makes overflow visually obvious | A bare line-count rule (gameable, no structure); per-skill bespoke templates (drift, no shared contract) | If observed gate messages still overflow with the slot caps in place |
| 2026-07-29 | Succinct-gates D1: one generic `REVIEW_GATE_TEMPLATE.html` on the dossier tokens serves every approval bookend, not per-gate templates | The dossier tokens generalized cleanly to a brief review on 2026-07-29 (live evidence); per-gate templates multiply the render-check surface for no structural gain | Per-gate templates (N render checks, N drift surfaces) | If a gate needs structure the generic slot regions can't express |
| 2026-07-27 | Output-style D5: reshape all four scattered terseness directives to defer to the style doc — `joycraft-interview` (1-2 pages), `joycraft-session-end` (ledger row), `joycraft-decide` and `joycraft-implement` (report intros). Nothing deleted; each keeps its concrete rule and gains a citation. Tests updated to allow several citations per skill, requiring at least one at an output moment. | The point is better agent prose, not a smaller diff. Each directive was correctly placed but stated its own notion of terse; citing the doc makes it the single home for that meaning while the local rule keeps the specifics the doc deliberately omits (rule 7 gives no numbers) | Deleting the directives (loses real information — "1-2 pages max" and the ledger's shape rule have no equivalent in the doc); leaving them (two homes for the same idea, the ONE_HOME condition); reshaping only 2 of 4 (the other two already sit under a pointer, but their bare "terse" still competed with the doc) | If a citation lands somewhere the agent does not read at write time — the test enforces placement, not attention |
| 2026-07-27 | Output-style D3: the pointer test asserts presence AND placement — each citation must sit under a report/present/playback heading, anchored to the nearest preceding heading, not to a following fenced block. No prose assertion. | Anchoring on the heading makes ordering provably uniform: all 15 pointers across the 11 skills already satisfy it, so no skill needs an exemption. The original framing rejected ordering as the suite's most fragile idiom, but that conflated a plain index comparison with the separate 1500-char windowed slice | Presence-only (ships nothing new, cannot catch a pointer buried by a later edit); fence-anchored ordering (would fail `tune` and `decide`, whose output moments are a written assessment and a bulleted summary rather than fenced templates); ordering only where a fence exists (two assertion classes plus an exemption list that drifts) | If a legitimately-placed pointer lands under a heading the phrase list does not cover — widen the list, never drop the assertion |
| 2026-07-27 | Output-style D2: the house style ships as a SHORT (~6-10 rule) positively-framed rule set with the motivation stated per rule — not a categorized banned-phrase list | Anthropic's July 2026 first-party guidance is the only evidence about the models Joycraft actually runs on, so it outranks star counts on skills never tested against frontier Claude; ship-and-measure, correct in a minor release | Full categorized banned-phrase list (the dominant published pattern — stop-slop ~14.6k stars — but contradicts first-party guidance for recent models and ships no eval); short rules + versioned tell list (hedges, but builds a maintenance obligation at every model bump on unproven content) | If observed report output shows the short rule set doesn't change behavior — then reach for the versioned tell list |
| 2026-07-27 | Output-style D1/B2: the style doc ships as bundled template content at `src/templates/reference/output-style.md`, landing in user projects at `docs/templates/reference/`; Joycraft-repo-specific guidance about its own self-hosting path asymmetry stays repo-local | The consumer of the library should not absorb Joycraft's self-hosting quirk — shipped skills must cite a path that resolves in a user project, and the repo can carry extra guidance for itself | Repo-internal `docs/reference/` only (dead pointer for every user; `skill-authoring.md` is the live precedent of a reference doc no skill cites); both homes split by audience (the ONE_HOME condition optimize exists to flag) | If user projects turn out not to want a house voice imposed by default |
| 2026-07-27 | Output-style B1: skill-body edits approved for this feature — style pointers may be added to human-facing skills, with bundle regen + installed-copy sync in the same commit | The living-harness/compound-engineering posture makes skills evolving over time an expected Joycraft behavior; without cited pointers the reference doc is inert, reproducing the uncited-doc failure mode | Reference doc with zero skill edits (smallest diff, no boundary crossing — but ships the appearance of a contract with no mechanism carrying it to the output moment) | If pointer edits prove to destabilize the position-fragile windowed content tests |
| 2026-07-21 | Living-harness D7: `decision-log.md` flips to newest-first (one-time reversal); write rule for time-ordered context docs becomes prepend-only, add-fact + decide stamping amended same-commit | A one-time churny diff is a fair price for making the most-read harness doc partial-read-safe — newest rows land where top-sampled partial reads actually reach | Keeping append-only with ledger-only newest-first (two opposite orderings — several versions of the truth); flipping at the 200-line breach (conditional convention, biggest diff at the worst time) | Never — one ordering convention is then the invariant |
| 2026-07-21 | Living-harness D6: the Reaper archive-moves undead feature folders to `docs/archive/features/`, proposed per folder and human-approved; deletion reserved for extracted (shipped) features | Drafts are often parked-not-abandoned — archive-move is the only recoverable disposal; undead folders had no extraction, so deleting loses the only copy (RF-KILL-7: squash merges break git-history recovery) | Symmetric deletion with the shipped path (loses never-extracted ideas) | If the archive accretes enough to need its own audit |
| 2026-07-21 | Living-harness D5: confidence anchors self-scored by authoring skills (design/new-feature) against `docs/context/anchors.md`; decide audits and may re-anchor | Separate author and judge as much as possible (RF-KILL-3); the downsides — two skills learn anchors, optimism until audit — are minor | Decide deriving all scores (certifies its own question selection; claims outside dossier scope stay unscored) | If self-scores prove persistently optimistic during the pilot |
| 2026-07-21 | Living-harness D4: harness config lives in committed `docs/.joycraft/config.json` beside gitignored state.json; encodes model tiers as `{stage: {intent, model}}`, per-harness maps, degradation ladder | `docs/.joycraft/` silos harness machinery away from human docs; committed = shared + reviewable while state stays machine-owned; JSON parses identically for skills now and src/ after pilot graduation | Extending state.json (gitignored — invisible to teammates, upgrade rewrites it); markdown harness.md (prose config every consumer must parse) | If the degradation ladder needs logic a flat file can't express |
| 2026-07-21 | Living-harness D3: harden pass = new `joycraft-harden` skill with `entry: agent` (invoked from tune's roadmap, optimize v2 dispositions, S9 promote; show-diff + approval retained). Resolved via new skill taxonomy riding S8: every skill declares `entry: human \| agent \| situational` frontmatter, internals get terse anti-discovery descriptions, optimize v2 enforces a human-door budget (≤9) + description budget; no renames | Punted mid-deposition, then resolved by the in-session taxonomy discussion: the scarce resource is human doors + the always-loaded description budget, not skill count — only ~7 of 21 skills are real doors, the rest are already reached via handoffs/skill-to-skill; terse internal descriptions reclaim budget for door vocabulary (discoverability) and prevent wrong auto-invocation | Folding harden into lockdown (interview-shaped vs convert-shaped flows muddle; only attractive when count was the cost); tune Tier 3 (buries a settings edit in the longest skill); renaming skills to encode the tier (breaks muscle memory + upgrade sync — [[2026-05-21]] setup-alias precedent); taxonomy-wide door redesign in-sprint (out of scope — S8 carries only the frontmatter field, description rewrite, and checks) | If the door list needs to grow past ~9, or headless drivers make the cross-conversation handoff boundary obsolete |
| 2026-07-21 | Living-harness D2: shipped ledger = live-head `docs/context/shipped.md`, newest-first, 200-line budget; over-budget rotates oldest rows to numbered shards (`shipped-001.md`…) with a pointer-only JSON manifest created at first rotation; decision-log shards the same way | Numbered shards rotate exactly at the budget (year shards are lumpy); a pointer-only manifest gives machine lookup without duplicating rows (one home per row); lazy creation avoids years of a near-empty index — the head won't rotate for ~190 shipped features | Per-feature `docs/ledger/` files (47 near-empty files; index duplicates rows); manifest from day 0 (dead machinery); plain file with no shard plan (re-opens the decision at breach time) | At the first real rotation — verify the mechanical manifest write works |
| 2026-07-21 | Living-harness D1: shipped feature folders deleted by the Reaper only after verified merge; session-end writes the ledger row + marks reap-eligible; `dossier.html` dies with the folder; deletion commits ride the next feature branch | Deletion never outruns PR review, and on protected mains the deletion commit piggybacks the next PR (collapsed in review via linguist-generated). The ledger row + decision rows are the extraction that makes deletion safe — resolves the "deleting exhaust post-merge" deferral in [[2026-07-16]] | Session-end deleting on self-verified merge (runs pre-merge — mostly no-ops, still needs the Reaper); pre-merge deletion with dossiers preserved (violates the done+merged rule, strips PR context) | If reap-eligible folders linger across many sessions without a PR to ride |
| 2026-07-20 | `implement-feature` (Claude) runs specs inline in the driving conversation; subagents only for parallel-safe waves and `isolated`-mode specs | Subagent-per-spec on sequential queues was pure orchestration overhead that hid the work from the conversation; subagents earn their place where they provide something — concurrency, or the fresh context `isolated` mode is defined by (PR #56) | Subagent-per-spec always (the prior design — context isolation as ceremony); killing the isolated-mode subagent too (would break the execution-mode semantics) | If context pollution over long inline queues measurably degrades late specs — then revisit per-spec isolation as the default |
| 2026-07-20 | Dossier rationale capture = Pattern B: ONE forced-choice question whose text instructs a free-text answer `"<choice> because <one-sentence reason>"` | Only pattern scoring 3/3 native-UI + typed rationale in the verify-question-capture spike; the free-text row doubles as the reject-framing escape (RF-KILL-6), and a single question never violates AskUserQuestion's ≥2-option minimum | Pattern A follow-up "Why?" question (1/3 — a one-option question is invalid, degrades to plain chat); Pattern C pick-then-note (works 3/3 but has a bare-pick gap; kept as documented fallback) | If AskUserQuestion gains a required-rationale/notes field or allows single-option questions |
| 2026-07-16 | Multi-tool installs: AGENTS.md is the single shared instruction file; CLAUDE.md is the `@AGENTS.md` import pointer (+ `## Claude Code` section). Claude-only installs keep the classic full CLAUDE.md | Claude Code never reads AGENTS.md natively (docs-verified); Anthropic's documented multi-tool pattern is the import. Two full siblings drift — this repo's own AGENTS.md was a stale TODO stub, proving it (PR #56) | Two full sibling files (drifted in our own repo); symlink (Windows needs admin/dev-mode); AGENTS.md importing CLAUDE.md (Codex import support unverified) | If Claude Code gains native AGENTS.md support, the pointer becomes optional |
| 2026-07-16 | PR docs bloat solved with `.gitattributes` marking workflow-exhaust paths (`docs/features`, `bugfixes`, `discoveries`, `templates`) `linguist-generated=true`; durable knowledge (CLAUDE.md, `docs/context/`, `docs/backlog/`) stays visible | Docs are two kinds of content: workflow exhaust is historical by PR time (specs were reviewed conversationally at creation); durable knowledge steers future agents and needs review eyes. GitHub collapses marked paths — reviewers see code + knowledge, exhaust is one click away. init + upgrade both apply it, append-only (PR #56) | Separate docs repo (breaks spec+implementation atomicity, agents need docs locally); splitting doc/code PRs (process overhead); deleting exhaust post-merge (deferred — only safe once session-end distillation is reliable) | If reviewers routinely need exhaust expanded, or GitHub changes linguist-generated rendering |
| 2026-07-16 | Level 5 de-emphasized to an experimental north star; the product story is excellent Levels 3-4 (spec-driven dev + a maturing harness) | Level 5 isn't feasible for most users with current models without serious budget and a maintenance team; "excellent L3-4" is claimable and demonstrable today. Tune Step 6 became a harness-maturity roadmap (PR #56) | Keeping L5 as the destination (invites the skepticism it deserves, misdirects tune's roadmap); removing L5 features entirely (they work — StrongDM proved the level) | If model economics/reliability make autonomous loops cheap enough for solo devs to maintain |
| 2026-06-15 | `init` now always creates `docs/backlog/` (with a README stub) alongside `docs/context/`, and the generated `CLAUDE.md`/`AGENTS.md` point at it | Capturing deferred work mid-sprint is core to Joycraft, so the generated harness files **should** reference `docs/backlog/` — which means the pointer must not dangle. Reverses the prior "only `context/` up front, everything else lazy" rule for this one dir (PR #54) | Keeping backlog purely lazy-created (leaves the generated pointer dangling until a skill first writes there); adding the pointer without scaffolding the dir (same dangle); not pointing at backlog from the harness files at all (loses the discoverability the whole feature is about) | If the up-front scaffold set grows further, reconsider whether "solo-first / lazy-create" is still the governing principle or just a default |
| 2026-06-15 | `init` offers interactive harness multi-select (claude/codex/pi); state relocated `.claude/.joycraft/` → `docs/.joycraft/state.json`; harness selection persisted so `upgrade` honors it | Resolves the exact risk the 2026-05-31 row flagged: once installs became harness-selectable, nesting state under `.claude/` forced a Claude footprint onto codex/pi-only projects. `docs/` is the one dir always created → harness-neutral home with zero new root entries (PR #52, v0.6.15) | New top-level `.joycraft/` dir (adds a root entry — defeats the "fewer files for squeamish devs" goal); state under the "primary" harness dir (needs a primary concept + relocation when harnesses change); keeping `.claude/.joycraft` (the smell itself) | If a harness is ever renamed/removed, the persisted `harnesses` list and `sanitizeHarnesses` fallback need a migration |
| 2026-06-14 | Retired specs graduate to `done` with a retirement notice in the body; no `retired` status invented | The unified `todo→in-review→done` vocabulary is the load-bearing invariant ([[2026-05-30]]). Adding `retired` as a fourth state breaks every status-validation test and replays the "two systems with different words" problem the migration solved. A retired spec IS "complete" in the sense that the decision was made and the work folded into other specs — `done` is the honest fit, the body explains | Inventing a `retired` status (tried mid-feature, broke `tests/status-migration.test.ts`); deleting the spec file entirely (loses the audit trail of why it was retired) | If we ever genuinely need a 4th state for un-shipped work, do a research pass on canonical project-management vocabularies first (don't invent) |
| 2026-05-31 | Upgrade-state hidden at `.claude/.joycraft/state.json` (gitignored, 16-char hashes), migrated-on-upgrade — keep per-file hashes, only relocate | Client complained about a committed `.joycraft-version` at the repo root; npm itself hides hashed state at `node_modules/.package-lock.json`. The hashes are what make "auto-update untouched files silently, prompt only on customized" work **offline** | Version-string-only baseline (would need old npm tarballs at upgrade time — breaks offline); shadcn copy-once+diff (prompts on every changed file — kills turnkey updates) | If a Codex-only/Pi-only install mode ever skips creating `.claude/` (the state location assumes it's universal) |
| 2026-05-30 | Unified spec status to `todo → in-review → done` across queue JSON + frontmatter | Two systems with different words (`active`/`complete` vs `shipped`/`backlog`) desynced on every change; researched canonical idiom (GitHub/Jira/Linear), not invented | Keeping two vocabularies (perpetual drift), a 4th `implemented`/`verified` state (no major tool tracks it; one more thing to drift) | Never — single vocabulary is the invariant |
| 2026-05-30 | Isolated-mode autonomous loop = single-shot `pi -p` process per spec; retired the in-process `joycraft_next_spec` Pi tool | The process boundary gives free, verified context isolation; the in-process TypeScript extension provably could NOT isolate context (context-isolation experiment) | Driving the loop from the in-process extension via `newSession`/`sendUserMessage` (tombstoned — leaks context) | If Pi adds a first-class in-process isolation primitive |
| 2026-05-30 | Per-spec **execution modes** (`batch`/`checkpoint`/`isolated`), hybrid-selected (project default + decompose recommendation, human-approved) | Specs aren't uniform — tiny ones want to batch, heavy ones need fresh context; one fixed wrap-up behavior is wrong. Human approves because risk isn't always visible from size | Auto-assign mode from size alone (a tiny spec in a risky feature may still need isolation — only a human knows) | If the size→mode heuristic proves reliable enough to drop the human gate |
| 2026-05-30 | Two-tier wrap-up: new lightweight `joycraft-spec-done` (per-spec: status bump + commit) + rescoped `joycraft-session-end` (once-per-feature: validation + push + PR) | Full session-end after every spec killed loop momentum; per-spec needs only a commit + status bump, the expensive cognition (validation, discovery consolidation, PR) belongs once per feature | A `--fast` flag on session-end (entangles the light path with heavy code, blurs the two tiers) | If a per-spec validation gate becomes necessary before `verify-in-loop` ships |
| 2026-05-21 | `/joycraft-setup` is a thin alias routing to `/joycraft-tune`, not a rename | Newcomer-facing door ("set up / get started") without breaking existing users' `tune` muscle memory or doc/memory references | Renaming tune→setup (breaks references), setup reimplementing detect+route (duplicates tune, drifts) | Never — alias is additive |
| 2026-05-21 | Lean CLAUDE.md + `## Context Map` pointer table; long-form refs in `docs/context/reference/` | Keep the always-loaded harness file small; link out to on-demand reference docs instead of inlining. tune flags CLAUDE.md >~200 lines (advisory) | One mega CLAUDE.md (bloats every session), one mega reference template (harder to scaffold specifically) | If pointer indirection proves more friction than inlining for small projects |
| 2026-03-26 | Fine-grained PAT via checkout `token` param for publish push | Bypasses branch protection for version bump commits; checkout action handles auth via HTTP headers | Embedding PAT in remote URL (breaks with fine-grained PATs), GitHub App (more setup for same result) | If GitHub deprecates PATs or adds native bypass for Actions |
| 2026-03-26 | Auto-publish on every merge to main | Eliminates manual version bumps and npm publish — every merge ships | Manual `npm publish` (easy to forget), separate release branch (overkill for solo/small team) | If we need release candidates or staging |
| 2026-03-25 | GitHub App token for autofix CI | Shows as distinct identity in PRs, cleaner than PAT | PAT (shows as repo owner, confusing), GITHUB_TOKEN (can't trigger workflows) | If GitHub changes App token permissions model |
| 2026-03-24 | Namespace skills as `joycraft-*` | Avoids collisions with other Claude Code plugins (superpowers, vercel, etc.) | Bare names like `tune`, `decompose` (collided immediately) | Never — namespacing is the right pattern |
| 2026-03-23 | Section-level CLAUDE.md merging over full replacement | Users customize their CLAUDE.md — overwriting destroys their work | Full replacement (loses user content), manual merge (users won't do it) | If merge logic becomes too complex to maintain reliably |
| 2026-03-23 | Copy skills as files, not symlinks | Skills must work after `npx joycraft init` without Joycraft installed | Symlinks (breaks without Joycraft), npm postinstall (fragile) | If skill updates become a major pain point (upgrade command mitigates this) |
| 2026-03-23 | Commander for CLI arg parsing | Minimal, stable, zero transitive deps — fits "keep it minimal" principle | yargs (heavier), meow (less maintained), raw process.argv (too manual) | If we need subcommand plugins or complex arg validation |

## Principles

- Prefer zero/minimal dependencies — this is a CLI tool users run via npx
- Templates and skills are the product — treat changes to them like API changes
- Append over modify when touching user files — never destroy existing content
- Eat your own dogfood — Joycraft's own repo uses Joycraft's harness
